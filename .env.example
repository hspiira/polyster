# Copy this file to .env and fill in your Supabase project's own values.
# Find both under Project Settings -> API in the Supabase dashboard.
# Never commit the real .env file.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Social sign-in buttons to offer, comma-separated. Only 'google' is supported.
# Leave empty unless the provider is actually enabled in Supabase -- there is no
# way to ask Supabase which ones are on, so this is what decides.
VITE_OAUTH_PROVIDERS=

# Set to 1 only once custom SMTP is configured. Without it the "forgotten my
# password" link is hidden, because a reset email cannot be delivered.
VITE_EMAIL_RECOVERY=

# Used only by scripts/set-shop-credentials.mjs (pnpm shop:credentials), never
# by the app. Project Settings -> API -> service_role. Bypasses RLS and can
# rewrite any user's credentials: no VITE_ prefix, treat it like a database
# password.
SUPABASE_SERVICE_ROLE_KEY=

# Used only by scripts/verify-rls.mjs (pnpm verify:rls), never by the app.
# Deliberately NOT prefixed with VITE_ -- Vite only inlines VITE_-prefixed
# vars into the client bundle, and this one must never end up there: it's
# the direct Postgres connection string (Project Settings -> Database ->
# Connection string), which authenticates as the `postgres` role and
# bypasses RLS entirely. Keep it out of anything shipped to a browser.
SUPABASE_DB_URL=postgresql://postgres:your-db-password@db.your-project-ref.supabase.co:5432/postgres
